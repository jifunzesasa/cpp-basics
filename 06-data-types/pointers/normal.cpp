#include <iostream>
using namespace std;

int main(int argc, char const *argv[])
{
    int *ptr = new int();

    // ptr->doSomething();
    //  We must do delete(ptr) to avoid memory leak
    

    return 0;
}
